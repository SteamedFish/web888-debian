# AGENTS.md — web888-debian

Project-specific rules for AI assistants and contributors.

## Project purpose

Run a **standard Debian (trixie, armhf)** system with a fully-featured kernel on the
Web-888 SDR (Zynq-7010, 512 MB RAM), root filesystem on the TF card instead of RAM —
with the closed-source driver/FPGA stack, WebSDR, and Red Pitaya software working
under Debian, switchable at runtime.

Boot chain: stock FSBL → **full U-Boot v2026.07** (SSBL) → boot.scr/uEnv.txt
+ dtb on the FAT firmware partition (`/boot/firmware`) → **Debian-source
6.12 kernel** ext4-loaded from the rootfs `/boot/zImage` symlink (host-built
pinned deb, `6.12.100-web888`; see `docs/dev/kernel-update-sop.md`).
The linux-xlnx 6.6 + stub-SSBL chain stays
buildable as rollback (`CHAIN=stub KERNEL=6.6 build-all.sh`).

**All code in this repository was generated by AI (Kimi K3) — 100% vibe coding.**
Review load-bearing claims against binaries and hardware before relying on them.

## Documentation map

- `docs/research/` — reverse-engineering knowledge base.
  `docs/research/hardware-facts.md` is the **authoritative, live-verified** source;
  everything else is analysis material — verify before relying on it.
- `docs/dev/` — development docs: `TODO.md`, `KNOWN-ISSUES.md`, `CHANGELOG.md`,
  procedures (kernel SOP), port guides, external research notes.
- `docs/user/` — user docs: `flashing.md` (start here), `usage.md`,
  `quick-reference.md`, `troubleshooting.md`.
- Each section has its own `README.md` index.

## Mandatory synchronization rules

**Every meaningful change MUST update, in the same commit:**

1. **`docs/dev/CHANGELOG.md`** — REQUIRED for every PR/commit that changes behavior,
   fixes a bug, adds/removes a feature, or changes documentation structure.
   The changelog restarted from zero at the first public release; do not backfill.
2. **`docs/dev/TODO.md`** — when completing or adding work items.
3. **`docs/dev/KNOWN-ISSUES.md`** — when a defect is found, fixed, or its status changes.
4. **`README.md` / `README.zh-CN.md`** — when features, usage, or layout change
   (keep the two in sync).
5. Tests and related documentation affected by the change.

## Hard constraints

- **Never touch the host NVMe disk.** Build artifacts go to `work/`/`output/`;
  flash targets are removable USB SD readers only (`/dev/sdX`), always behind the
  guards in the flash script.
- **Blind testing only** — no serial adapter. QEMU boot test gates every hardware
  flash (`scripts/test-qemu.sh uboot`). The stock TF card stays untouched in
  storage as the rollback.
- **Scratch chroots with bind mounts** — keep throwaway repro chroots under
  `/tmp`, not `.tmp/`; never `rm -rf` a directory that ever hosted bind mounts
  without `umount -R` + `mountpoint -q` verification first, and repro scripts
  must unmount via an EXIT trap. An orphaned `/dev` bind turns cleanup into
  deletion of the HOST's device nodes (happened 2026-08-16: killed every
  shell on the host, forced a reboot — postmortem:
  `docs/dev/github-ci-apt-repo.md` §9).
- The stock kernel has **no ext4** — a custom kernel is mandatory from the first boot.
- Build tooling runs on a Linux host; `scripts/env-setup.sh` verifies the
  required toolchain.
- **Project license is GPL-2.0-or-later** (see `LICENSE`). All new code must use
  SPDX `GPL-2.0-or-later` (files relicensed from the Xilinx reference tree, which
  is GPL-2.0-only, remain GPL-2.0-only and stay in `resources/reference/`).
  Vendored reference under `resources/reference/` keeps its original upstream
  license as documented in each `PROVENANCE.md`.

## Git workflow

- The private primary remote lives on forgejo; the public GitHub mirror is
  published from the cleaned orphan `master` (see `docs/dev/CHANGELOG.md`
  first entry).
- The pre-cleanup history is archived on the forgejo-only branch
  `master-pre-cleanup` — **never push it to GitHub**.
- Work on `master` or short-lived feature branches; keep commits atomic.
- `docs/` is tracked; `.tmp/`, `work/`, `output/` are gitignored.
- Do not commit personal/local files unless they are already tracked.

## Directory layout

```
docs/
  research/   reverse-engineering knowledge base (hardware-facts.md = verified)
  dev/        TODO.md, KNOWN-ISSUES.md, CHANGELOG.md, procedures, port guides
  user/       flashing.md, usage.md, quick-reference.md, troubleshooting.md
scripts/    build/flash/test scripts (tracked)
config/     kernel/u-boot/devicetree/config fragments (tracked)
packaging/  debian packaging: web888-websdr, web888-redpitaya (tracked)
work/       build trees: kernel, u-boot, busybox, debootstrap (gitignored)
output/     final artifacts: zImage, dtb, boot.bin, rootfs, images (gitignored)
resources/  vendored non-generated build inputs (tracked): stock boot.bin, busybox deb
.tmp/       scratch + stock firmware analysis dumps (gitignored)
```

## Device access

`ssh -p 22 root@<device-ip>` (password `changeme`; use `sshpass` for scripted
access). **The IP is DHCP-assigned and floats** — never hardcode it.
Pass `-p 22` explicitly if the client's ssh config defaults `Port` to a
nonstandard value — a bare `ssh root@ip` may otherwise connect to the wrong
port.

**Discovery: mDNS `web888.local`** (avahi-daemon on the Debian image, IPv4 +
IPv6). Fallback when mDNS is unavailable: MAC prefix `ce:cf:3f:*` via
`sudo nmap -sn <lan-subnet>` or `ip neigh | grep -i ce:cf:3f`, or the router's
DHCP lease table. The prefix is stable. Images present the per-unit MAC read
from the board EEPROM. See `docs/research/hardware-facts.md` for details.
